{
  "build": {
    "env": {
      "APP_ENV": "production"
    },
    "command": "composer install --no-dev --optimize-autoloader && npm install && npm run build && echo 'Checking DB...' && if [ -f database/database.sqlite ]; then echo 'Copying SQLite DB...'; mkdir -p /tmp && cp database/database.sqlite /tmp/database.sqlite && chmod 666 /tmp/database.sqlite; else echo 'SQLite DB not found'; fi"
  },
  "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
}
