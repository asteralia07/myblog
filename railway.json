{
  "build": {
    "env": {
      "APP_ENV": "production"
    },
    "command": "composer install --no-dev --optimize-autoloader && npm install && npm run build && mkdir -p /tmp && cp database/database.sqlite /tmp/database.sqlite && chmod 666 /tmp/database.sqlite"
  },
  "start": "php artisan serve --host=0.0.0.0 --port=$PORT"
}
